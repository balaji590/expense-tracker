{
  "name": "expense-tracker-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend skeleton for ExpenseTracker (Phase 5.1) - Express + PostgreSQL, no auth yet.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db/migrate.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.13.1",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5",
    "helmet": "^7.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
